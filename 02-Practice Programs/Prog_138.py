# Create a program to display the current working directory and then display all files and folders present in that directory using os.getcwd() and os.listdir().

import os
print(os.getcwd())
print(os.listdir())